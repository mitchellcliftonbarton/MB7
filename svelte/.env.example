# Kit (formerly ConvertKit) form ID for the newsletter signup.
# Find it in Kit: Grow > Landing Pages & Forms > your form > the numeric ID in the URL/embed code.
# Public (PUBLIC_ prefix) — it's exposed to the browser, which is expected for Kit's embed endpoint.
PUBLIC_KIT_FORM_ID=
